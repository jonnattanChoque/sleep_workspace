import 'package:flutter/material.dart';

class TwonDSColors {
  // Colores principales (Inspirados en la noche/sueño)
  static const Color primaryNight = Color(0xFF1A237E); // Azul profundo
  static const Color accentMoon = Color(0xFFFFD54F);    // Amarillo luna
  static const Color partnerColor = Color(0xFF81C784);  // Verde suave para la pareja
  static const Color error = Color(0xFFD32F2F);   // Un rojo que combine con tu paleta
  static const Color success = Color(0xFF388E3C); // Un verde que combine
  
  // Fondos y superficies
  static const Color background = Color(0xFF0F172A);
  static const Color surface = Color(0xFF1E293B);
  
  // Calidad de sueño
  static const List<Color> qualityGradient = [
    Color(0xFFEF4444), // Malo
    Color(0xFFF59E0B), // Regular
    Color(0xFF10B981), // Excelente
  ];
}